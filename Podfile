# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HOOKD' do
    
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HOOKD
  target 'HOOKDTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HOOKDUITests' do
    inherit! :search_paths
    # Pods for testing
    
  end
  pod "CDAlertView"
  pod "SKPhotoBrowser"
  pod 'SDWebImage', '~> 4.0'
  pod 'OpenTok'
  pod 'FontAwesome.swift'
  
end
